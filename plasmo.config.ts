module.exports = {
    host: "localhost", // Explicitly set the host
    port: 3000, // Ensure the dev server uses a supported port
    runtime: {
      hmr: true, // Enable Hot Module Replacement
    },
  };
  